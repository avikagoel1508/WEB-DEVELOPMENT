let arr=[1,2,3,4,5,6];
for (var i = 0; i < arr.length; i++) {
    console.log(arr[i]);
    //push-> to insert at end
    //pop-> to remove from end
    //unshift-> insertion in starting of arr
    // shift-> same but remove

}
arr.push(2000);
console.log(arr);